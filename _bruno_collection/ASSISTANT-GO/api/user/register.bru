meta {
  name: register
  type: http
  seq: 1
}

post {
  url: {{domain}}/api/user/register
  body: json
  auth: none
}

headers {
  locale: ru
}

body:json {
  {
    "login": "{{login}}",
    "password": "{{password}}"
  }
}
